<% provide(:title, "All users") %>
<h1>All users</h1>

<%= paginate @users %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 80 %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>